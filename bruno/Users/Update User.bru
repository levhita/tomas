meta {
  name: Update User
  type: http
  seq: 4
}

put {
  url: {{server_url}}/users/:id
  body: json
  auth: inherit
}

params:path {
  id: {{userId}}
}

body:json {
  {
    "username": "admin",
    "password": "admin"
  }
}

script:pre-request {
  const userId = await bru.prompt('userId', 'userId', '')
  bru.setVar("userId", userId);
}
