meta {
  name: Create User
  type: http
  seq: 3
}

post {
  url: {{server_url}}/users
  body: json
  auth: inherit
}

body:json {
  {
    "username": "admin",
    "password": "admin"
  }
}

script:post-response {
  bru.setVar("userId", res.body.id);
}
