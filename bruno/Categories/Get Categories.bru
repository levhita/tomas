meta {
  name: Get Categories
  type: http
  seq: 1
}

get {
  url: {{server_url}}/categories?workspace_id={{workspaceId}}
  body: none
  auth: inherit
}

params:query {
  workspace_id: {{workspaceId}}
}
